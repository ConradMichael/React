import React, { Component } from 'react';
import '../Trainer.css';

class Back extends Component {
	render() {
		return (
		<div>
			<button class="navButton"> Back </button>
		</div>
    );
  }
}

export default Back;
